// A constructor Triangle is created.
function Triangle() {};

// Constructor Triangle is given a setColor method that sets the color of the triangle.
Arithmetic.prototype.setColor = (color) => {
  
};

module.exports = Triangle;